meta {
  name: Add user to a cercle
  type: http
  seq: 3
}

post {
  url: http://localhost:3000/cercle/{{cercleId}}/user
  body: json
  auth: inherit
}

body:json {
  {
    "cercleId": "{{userId1}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
