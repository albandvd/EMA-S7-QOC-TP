meta {
  name: Create User 2
  type: http
  seq: 2
}

post {
  url: http://localhost:3000/user
  body: json
  auth: inherit
}

body:json {
  {
    "nom": "David",
    "prenom": "Alban"
  }
}

tests {
  test("User créé", () => {
    expect(res.status).to.equal(201)
  
    const body = res.body
    expect(body).to.have.property("userId")
    const userId = body.userId
    bru.setVar("userId2", userId)
  })
}

settings {
  encodeUrl: true
  timeout: 0
}
