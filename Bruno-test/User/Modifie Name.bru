meta {
  name: Modifie Name
  type: http
  seq: 5
}

put {
  url: http://localhost:3000/user/{{userId1}}
  body: json
  auth: inherit
}

body:json {
  {
    "nom": "Goatlavong",
    "prenom": "Laurent"
  }
}

tests {
  test("User récupéré", () => {
    expect(res.status).to.equal(200)
    expect(res.body).to.have.property("prenom", "Laurent")
    expect(res.body).to.have.property("nom", "Goatlavong")
  })
  
}

settings {
  encodeUrl: true
  timeout: 0
}
