meta {
  name: Create User
  type: http
  seq: 1
}

post {
  url: http://localhost:3000/user
  body: json
  auth: inherit
}

body:json {
  {
    "nom": "Boualavong",
    "prenom": "Laurent"
  }
}

tests {
  test("User créé", () => {
    expect(res.status).to.equal(201)
  
    const body = res.body
    expect(body).to.have.property("userId")
    const userId = body.userId
    bru.setVar("userId1", userId)
  })
}

settings {
  encodeUrl: true
  timeout: 0
}
