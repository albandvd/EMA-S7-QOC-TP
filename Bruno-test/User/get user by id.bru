meta {
  name: get user by id
  type: http
  seq: 4
}

get {
  url: http://localhost:3000/user/{{userId1}}
  body: none
  auth: inherit
}

tests {
  test("User récupéré", () => {
    expect(res.status).to.equal(200)
    expect(res.body).to.have.property("prenom", "Laurent")
    expect(res.body).to.have.property("nom", "Boualavong")
  })
  
}

settings {
  encodeUrl: true
  timeout: 0
}
